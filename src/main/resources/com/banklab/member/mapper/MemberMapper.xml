<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.banklab.member.mapper.MemberMapper">
    <resultMap id="authMap" type="AuthVO">
        <result property="memberId" column="member_id" />
        <result property="auth" column="auth" />
    </resultMap>
    <resultMap id="memberMap" type="MemberVO">
        <id property="memberId" column="member_id" />
        <result property="username" column="username" />
        <result property="password" column="password" />
        <result property="name" column="name" />
        <result property="phone" column="phone" />
        <result property="gender" column="gender" />
        <result property="birth" column="birth_date" />
        <result property="email" column="email" />
        <result property="regDate" column="reg_date" />
        <result property="updateDate" column="update_date" />
        <collection property="authList" resultMap="authMap" />
    </resultMap>
    <!-- 회원 정보 조회 (LEFT JOIN으로 권한 정보 포함) -->
    <select id="get" resultMap="memberMap">
        SELECT m.member_id, username, password, name, phone, email, gender, birth_date, email, reg_date, update_date, auth
        FROM
            member m
                LEFT OUTER JOIN member_auth a
                                ON m.member_id = a.member_id
        where m.username = #{username}
    </select>
    <!-- 회원 정보 저장 -->
    <insert id="insert">
        INSERT INTO member (username, password, name, phone, email, gender, birth_date, reg_date, update_date)
        VALUES(#{username}, #{password}, #{name}, #{phone}, #{email}, #{gender}, #{birth}, now(), now())
        <selectKey resultType="Long" keyProperty="memberId" keyColumn="member_id" order="AFTER">
            SELECT LAST_INSERT_ID()
        </selectKey>
    </insert>
    <!-- 권한 정보 저장 -->
    <insert id="insertAuth">
        INSERT INTO member_auth(member_id, auth)
        VALUES(#{memberId}, #{auth})
    </insert>
    <!--  ID 중복 체크용 조회  -->
    <select id="findByUsername" resultType="MemberVO">
        SELECT * FROM member WHERE username = #{username}
    </select>
</mapper>